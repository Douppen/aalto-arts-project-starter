version: "3.8"

services: 
  database:
    container_name: postgres-dev
    image: postgres:14.1
    restart: "no"
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=username
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=database